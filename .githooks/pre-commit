#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"

echo "[pre-commit] running hermetic test suite..."
python -m pytest "$ROOT/tests" -q --ignore="$ROOT/tests/test_real_tool_conformance.py" --ignore="$ROOT/tests/test_e2e_download.py"

echo "[pre-commit] running companies-reuse-only E2E..."
python "$ROOT/e2e/run_companies_reuse_only_e2e.py"

echo "[pre-commit] checking skill installation sync (R4.2)..."
python "$ROOT/tools/sync_installs_b3.py" --check
