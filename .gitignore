__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
.benchmarks/
# Windows reserved device name: guards against redirect accidents (e.g. `> nul`).
nul
# CodeGraph structural index (regenerable).
.codegraph/
# Audit / planning working notes (not part of the skill install surface —
# tracked in git so the WU-8.3 plan verifier gate has them in CI).
# findings.md / progress.md / task_plan.md are version-controlled.

# E2E run artifacts (regenerable)
e2e/.runs/
